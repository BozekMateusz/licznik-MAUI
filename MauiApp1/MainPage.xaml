<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            
            <Entry
                x:Name="EnterName"
                Placeholder="enter name for counter"/>
            
            <Button
                x:Name="btnAddNewCounter"
                Text="Add New Counter"
                Clicked="OnAddCounterClicked"
                HorizontalOptions="Center" />
            
            <VerticalStackLayout
                x:Name="CountersContainer"
                Spacing="20"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
